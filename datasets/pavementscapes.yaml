path: /workspace/data/processed/pavementscapes
train: images/train
val: images/val
# masks should mirror images path (labels/train, labels/val) in YOLO format

names:
  - crack
  - pothole
  - patch
  - repair
  - marking
  - background
# note: using YOLO segment task